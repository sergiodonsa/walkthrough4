// @ts-ignore
sap.ui.define([
        "sap/ui/core/mvc/Controller"
    ],

	function (Controller) {
		"use strict";
		return Controller.extend("crmo.walkthrough4.controller.View1", {

        onInit : function () {
             // set data model on view
            var oData = {
                recipient : {
                    name : "World",
                }
            };
        },
         
		});
	});
